Add index to product related models.

This module is designed to optimize database queries by adding indexes to product-related models. 
It improves query performance, especially for large datasets, by reducing the time required to retrieve data.
